import 'package:flutter/material.dart';

/// Centralised colors so every screen looks consistent.
/// Matches the RouteMatic brand: teal logo, blue action color.
class AppColors {
  static const Color primaryBlue = Color(0xFF1E6FBA);
  static const Color darkBlue = Color(0xFF14508F);
  static const Color teal = Color(0xFF2AA79A);
  static const Color background = Color(0xFFF5F6F8);
  static const Color cardGreen = Color(0xFFE6F4EE);
  static const Color textDark = Color(0xFF1A1A1A);
  static const Color textGrey = Color(0xFF6B6B6B);
  static const Color danger = Color(0xFFE04141);
  static const Color success = Color(0xFF2FA84F);
}
